# Research

Research artifacts live in `research/`.

## Contents

- `starter_notebook.ipynb`: Empty starter notebook scaffold
- `pickles/`: Storage for local model artifacts (`.pkl`)

## Git Tracking Rules

- `research/pickles/.gitkeep` keeps the directory in git
- `research/pickles/.gitignore` ignores `*.pkl` files
